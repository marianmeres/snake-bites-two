/**
 * Will render multiple boards horizontaly side by side, so it's easy to visualy compare
 * @param renderedBoards
 * @param labels
 */
export declare const renderMultipleBoards: (renderedBoards: string[], labels?: string[]) => string;
